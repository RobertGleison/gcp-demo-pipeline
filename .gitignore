# ---- Secrets / local env (we use GCP Secret Manager; nothing secret in git) ----
.env
.env.*
*.key
*-key.json
*-credentials.json
service-account*.json
gha-creds-*.json

# ---- Terraform ----
.terraform/
# Commit the lock file: pins exact provider versions/hashes for reproducible CI runs.
!.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
!example.tfvars
# Track the shared, non-secret env config (project_id/region/bucket/repo — no secrets).
!terraform/environments/**/common.tfvars
!terraform/environments/**/terraform.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- OS / editor ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
